package com.example.ejemplo.Controlador;

import com.example.ejemplo.R;

import java.net.URI;

public class getDistanceMatrixAPI {
    public static float [][] distance;
    public static float [][] times;

    public static void getData(String origin, String destination)throws  Exception{
        /*var url = "https://maps.googleapis.com/maps/api/distancematrix/json?origins=" + origin + "&destinations=" + destination + "&key=" + R.string.googleAPIKEY;
        var request = HttpRequest.newBuilder().GET().uri(URI.create(url)).build();
        var client = HttpClient.newBuilder().build();
        var response = client.send(request, HttpResponse.BodyHandlers.ofString()).body();*/
    }
}
